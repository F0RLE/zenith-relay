services:
  relay:
    build:
      context: ..
      dockerfile: relay-server/Dockerfile
    restart: unless-stopped
    env_file: .env
    ports:
      - "14999:14999"
    volumes:
      - relay-data:/var/lib/zenith-relay

volumes:
  relay-data:
